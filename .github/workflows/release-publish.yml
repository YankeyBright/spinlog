name: Release Bootstrap

on:
  push:
    tags: ['v0.2.0']

permissions:
  contents: read

jobs:
  release-build:
    name: Build And Attest Bootstrap Artifact
    uses: ./.github/workflows/release-build.yml
    permissions:
      contents: read
      id-token: write
      attestations: write
