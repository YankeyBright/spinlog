{
  "name": "spinlog",
  "version": "0.1.0",
  "description": "A sub-1.2kB, zero-dependency, ESM-only unified primitive that replaces chalk + ora.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "sbom.json"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "spinlog contributors",
  "repository": {
    "type": "git",
    "url": "https://github.com/spinlog/spinlog.git"
  },
  "keywords": [
    "cli",
    "spinner",
    "terminal",
    "ansi",
    "colors",
    "esm",
    "zero-dependency",
    "supply-chain",
    "sbom"
  ],
  "scripts": {
    "build": "npm run build:js && npm run build:types",
    "build:js": "tsup",
    "build:types": "tsc --emitDeclarationOnly",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format . --write",
    "sbom": "cyclonedx-npm --package-lock-only --validate --output-file sbom.json --spec-version 1.5 --output-reproducible --omit dev --omit optional --omit peer --mc-type library",
    "sbom:check": "node scripts/check-sbom.mjs",
    "policy:check": "node scripts/check-package-policy.mjs",
    "size": "node scripts/check-size.mjs",
    "size:limit": "size-limit",
    "pack:check": "node scripts/check-pack.mjs",
    "check:phase-map": "node scripts/check-phase-map.mjs",
    "check:phase0": "npm run check:phase-map && node scripts/check-phase0.mjs",
    "check:phase1": "npm run check:phase-map && npm run policy:check && npm run typecheck && npm run lint && npm run test:coverage && npm run build && node scripts/check-phase1.mjs && npm run size && npm run size:limit && npm run pack:check",
    "check:phase1:release": "npm run sbom && npm run sbom:check && node scripts/check-phase1-release.mjs",
    "check:phases": "node scripts/check-phases.mjs",
    "verify": "npm run policy:check && npm run typecheck && npm run lint && npm run test:coverage && npm run build && npm run size && npm run size:limit && npm run pack:check",
    "verify:release": "npm run verify && npm run sbom && npm run sbom:check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@cyclonedx/cyclonedx-npm": "6.0.0",
    "@size-limit/preset-small-lib": "11.2.0",
    "@vitest/coverage-v8": "3.2.7",
    "size-limit": "11.2.0",
    "tsup": "8.5.1",
    "typescript": "7.0.2",
    "vite": "6.4.3",
    "vitest": "3.2.7"
  },
  "overrides": {
    "tsup": {
      "esbuild": "0.28.1"
    }
  }
}
